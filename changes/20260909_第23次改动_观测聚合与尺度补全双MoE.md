# 第23次改动：观测聚合与尺度补全双 MoE

- 修改日期：2026-09-09
- 开发分支：`v23`
- 修改前提交：`1f348d4`（当前分支 HEAD）
- 修改后提交：`待本次工作区提交后回填`
- 对应说明：`model_designs/v23_双MoE聚合与尺度补全模型.md`

### 2026-09-14新增：后端8个路由专家＋1个共享专家，Top3

新增可选routed_shared布局及中央dual_moe_shared_topk.json，沿用train_scale_completion.py，不新建版本模型目录或训练脚本。前端E8/Top4及三尺度特征提取保持，8个独立后端专家读取全部尺度，输出修正；共享专家给公共基础预测，始终启用且不计入TopK。最终shared+加权Top3残差，不再使用L的等权回退。

旧scale/D/H/L模式保持默认行为、原参数键及初始化；新配置独立保存。现有辅助损失仍约束三个尺度头，后端均衡配置为0.001且只统计8个路由专家。日志改为e0–e7、共享预测和共享加单修正误差；可选诊断支持8专家相关/胜率，避免旧三尺度标签与维度错误。

新增结构/Top3归一化/选中与未选中梯度/共享始终参与/均衡范围/前端初始化一致/无真值泄漏/日志/三数据集形状与CPU训练恢复检查。没有启动正式训练。此改动同时改变后端容量与共享残差机制，不应当作仅改变专家数量的纯消融。

## 首版修改记录（固定半径，已被文末修订替换）

1. 在现有 `models/` 目录实现 `DualMoEBackbone`，不建立 v22 或版本化模型子目录，不包装/实例化 V14 网络。
2. 前端在 stride 2/4 上分别选择半径 0/1/2 聚合算子，只聚合观测来源；不复用其权重做回传。
3. 中/粗尺度独立处理、固定空间回传并结合细上下文；后端逐位置选择 fine/mid/coarse 补全预测。
4. 传递观测分配质量、有效权重数量和异质性统计；重复专家边不能重复计算同一观测。统计停止梯度，不冒充置信概率。
5. 新架构只用归一化缺失重建目标，历史 V14/跨尺度/平衡损失不进入 V23；可配置两类路由的 learned/static/uniform 消融。
6. 接入现有 train/evaluate、原始范围指标、精确路由日志和单个最佳检查点。原子覆盖检查点，正式 V23 训练必须提供独立 train/val/test 路径，不能静默合成数据。
7. 默认 preset 切换到 V23，公共覆盖配置放在现有 `configs/presets/`；历史 main/V14 架构和配置仍可显式使用。输出隔离到 `outputs/v23/`。

## 保留事项

未切换分支、未提交、未删除或重置历史代码/实验。开始时分支为 v23，但工作区已有未提交的聚合探针脚本、配置、文档和 baseline 子模块内部文件；这些原有内容保留。历史 README 的既有修改也保留。

## 验证边界

通过新模型及历史模型的回归测试、GPU AMP 检查、三个真实数据集 fixed/random 的小批次更新/验证/最佳重载/测试，以及现有训练入口的两轮完整合成 smoke。正式模型效果尚未验证，本次不把流程检查指标视为论文实验结果。

## 同日修订：由 MoE 学习聚合形状（当前实现）

用户明确要求不预设聚合形状，因此替换首版固定锚点/半径结构，仍属于 V23，不新增版本模型目录。

1. 每个尺度的每个聚合专家学习独立细→粗分配矩阵，默认 3 个专家，中/粗节点数 32/8。区域成员、软形状由观测上下文/坐标/mask 学习；没有固定半径或距离约束，但也不保证连通。
2. 各专家按自己的分配和观测源门控独立聚合；不混合编号相同却不对应的区域。粗节点改用置换等变的节点注意力、共享时间卷积、前馈残差，不使用粗空间网格卷积。
3. 每个专家按自身分配恢复细网格后，才按观测源门控均值组合；后端独立融合细/中/粗预测。保留单阶段端到端训练。
4. 增加可关闭的分配互信息正则（权重 0.001），以及分配熵/有效区域数日志。增加真正单可学习聚合器配置 aggregation_experts=1；uniform 源路由不等于关闭分配学习。
5. 配置使用 coarse_nodes、aggregation_experts、assignment_temperature，拒绝旧 strides 配置。新输出 outputs/v23/learned_regions/，与首版分开；不删除原输出和历史文档记录。
6. 新模型 21 项测试通过（含 CUDA AMP、TaxiBJ/BikeNYC/CHAP 的 fixed/random @0.4 真实分割各一批训练→验证→保存重载→测试）。额外覆盖区域编号置换、专家各自回投、分配梯度、无真值泄漏、1/2/3 专家和可关闭正则。

尚未执行全量效果对照。可训练和形状可学习不代表已经获得稳定提升或独立创新性证据。

最终回归：unittest discover 共 73 项，71 项通过、2 项 GPU opt-in 跳过；这 2 项已在单独开启 opt-in 的新模型 21 项测试中通过。现有 scripts/train.py 的两轮合成训练流程完成，验证 2 次，重载 epoch 2 的最佳参数后测试 1 次，检查点目录只有 best.pt。日志保留于 outputs/v23/learned_regions/smoke/Synthetic/debug/smoke_learned_regions/random/rate0.4/20260909_135728_seed42_bs2/。

## 四组对比入口

新增 scripts/run_dual_moe_comparison.py、configs/presets/dual_moe_comparison.json 和对应测试，均放在现有目录。四组只改变源专家门控和尺度门控的 uniform/learned，区域分配始终学习。三个数据集 fixed/random @0.4、seed42，共 24 次；统一 80 epoch、val_epoch=2、train≤512、完整 val/test、单 GPU。具备真实批次计时/截止准入、严格跳过完整实验、代码与数据状态指纹、实时日志和结果汇总。没有启动这批 24 次正式实验，仅做耗时校准及独立的小预算入口测试。

对比脚本 5 项测试通过，包括 BikeNYC random 四组各两轮真实训练、完整验证/测试、第二次执行四组均跳过。端到端检查修正了两处问题：训练入口实验索引支持仓库外输出路径；完成判断区分 metrics.jsonl 中的 epoch 行与最终 test 行，要求唯一 test 记录且 best_epoch 与验证最优轮一致。现有 78 项回归测试通过（3 个显式 opt-in 项跳过；本次对比入口 opt-in 已单独执行通过）。

## 三阶段机制诊断入口

新增 scripts/run_dual_moe_diagnostics.py、configs/presets/dual_moe_diagnostics.json 和测试。默认1→2→3：12个已有A01/A11检查点×9次完整验证干预；6次单聚合器训练；24次弱分支监督训练。参考80轮/val2/seed42/相同训练窗口预算，完整val/test，复用原0监督对照；不改模型/损失/训练主程序源码，不覆盖已有实验。

干预通过临时hooks检查中/粗特征置零、回投空间错配、粗路径细上下文置零，并加槽编号置换阴性对照。保存分支MAE、平均偏差、加权误差抵消比例与路由统计。明确这是验证集敏感性诊断，不等同重训练消融；单聚合器参数量与初始化调用序列也不与三专家完全相同。

支持源码/数据/参考结果审计、指纹隔离、文件锁、分阶段执行、完整任务跳过、唯一最佳参数、实时日志、JSON/CSV汇总。已有小预算真实数据端到端测试覆盖三阶段及第二次跳过；正式30次训练队列未启动。

## 基于已完成诊断的优化候选：anchored_scale_moe

用户随后完成 diagnostics/c580deec7b3d5918 全部三阶段结果：粗特征确有依赖，多个聚合器在 TaxiBJ random 更有效；0.01分支监督减少抵消且使A11六点验证误差方向一致改善，但不证明前端动态门控普遍必要。本次依据这些结果新增候选，不直接宣称已获得性能提升。

1. 在原 dual_moe.py 中新增 AnchoredScaleMoEBackbone，通过 model.dual_moe.design 选择。旧类及默认设计保持原行为，旧配置和输出不替换。
2. 前端默认保留三个可学习聚合器，均匀组合；不再学习第二套路由权重。另有规则、观测归一化空间分组模式（步幅4/8），以紧凑索引回传，不分配稠密one-hot成员矩阵。
3. 中/粗分支输出共享细预测的有界修正，默认上限为一个观测归一化尺度。修正头零初始化，后端MoE选择不修正/中修正/粗修正。限制相对细预测的自由漂移，但不保证误差改善或消除全部抵消。
4. 使用已有分支重建损失，默认0.01；不引入教师、多阶段训练或额外复杂损失。增加修正幅度和饱和率诊断日志。
5. 新配置 scale_completion.json / scale_completion_grid.json；新入口 scripts/train_scale_completion.py，支持三数据集、fixed/random四种缺失率、单卡和可选匹配旧预算的训练窗口子集。输出独立到 outputs/v23/scale_completion/。
6. 90项回归测试通过（5项opt-in跳过）；两种聚合×三真实数据集×fixed/random 的CUDA AMP小批次训练/验证/重载/测试已单独通过。现有训练入口两轮合成流程通过，唯一最佳参数及新增日志正常。旧BikeNYC random A01检查点重载后验证MAE 3.00011882，与原3.00011271一致至约2×10^-6相对误差。

本次未执行优化模型的正式80轮效果实验。旧诊断入口因源码指纹改变会按原有规则拒绝混用旧协议，但旧模型加载及历史实验文件仍保留。

## 优化候选的三种子与机制对照队列

新增 `scripts/run_scale_completion_experiments.py` 与 `configs/presets/scale_completion_experiments.json`，不改模型结构或历史实验配置。三数据集 fixed/random@0.4，B00/B01/N01 三主方案各三种子，N00/NU/NG/NB2 四消融各 seed42，共78次。统一120epoch、val_epoch=2、batch4、最多512训练窗口，验证和测试完整。使用现有最佳验证参数覆盖保存及最终重载测试流程。

脚本支持七方案分数据集真实测时、北京时间截止准入、同协议完成跳过、失败停止、完整训练/验证/测试日志、CSV/JSON逐组结果及同seed配对百分比与均值/标准差。新队列全部重训参照，避免混合旧80轮和新120轮。输出仍集中在 `outputs/v23/scale_completion/comparison/`。

新增5项测试均通过，包含七方案各真实2epoch的端到端验证和再次执行跳过。正式78组尚未启动；GPU0测时加1.6倍余量约8.45小时。实验设计、比较限制和命令记录在 `model_designs/v23_尺度修正MoE对照验证方案.md`。

## 2026-09-10：按用户要求加入双Top-K和有效负载均衡

在B01原独立尺度预测上新增可配置Top-K，前端每尺度3专家K=2、后端3尺度专家K=2；原dense/static/uniform行为和旧配置保留。完整softmax概率参与可微Switch-style均衡，actual selected计数detach并按总分派数归一化。前端仅观测源位置、后端仅缺失目标位置，前端两个尺度平均；前/后系数均默认0.01，独立配置/消融，明确接入dual_moe实际损失路径。日志同时保存原始/加权均衡项、概率、激活率、实际负载与K。

`configs/presets/dual_moe_topk.json` 和已有启动器新增的 `--preset dual_moe_topk` 启用本候选，输出 `outputs/v23/topk_dual_moe/`。未覆盖历史模型，未启用之前撤回的局部读出改动。Top-K在前端按源节点选专家，空间prior仍可能使用全部专家；不冒充目标级组织Top-K。当前专家计算仍为批量dense执行，不声称加速。K=1有明确straight-through近似，默认K=2是选中logits的标准softmax。

新增测试检查实际稀疏选择、均衡反向梯度、过载方向、屏蔽统计、旧参数兼容、日志与三数据集真实小批次训练/验证/重载/测试。未执行正式120轮训练，不能据此宣称双MoE精度或必要性已成立。

## 2026-09-10：全量公平对照后的前端专家数量探索

用户完成全量2491样本五组对照后，T11 test MAE=13.1246、T01=13.1513；前端额外收益仅约0.20%，关闭双均衡的T11_NB=13.7421且前端第三专家观测分派为0。以上是单种子观察，不是显著性结论。

按用户要求只探索前端专家数：模型解除原1/2/3限制，接受正整数，不改变聚合映射、读出、后端结构、优化器或旧预设。新增 `configs/presets/dual_moe_expert_count.json`，复用 `scripts/run_scale_completion_experiments.py`，E=3/4/6/8分别配等权/Top-2，后端固定3专家Top-2，均衡系数固定，共8组同预算实验；原三专家结果/配置不覆盖。

协议校验强制保留同E对照，禁止单组更改训练预算。汇总同时给出容量变化和同E前端路由收益，以及相对E3的收益变化。路由日志支持大专家池，按专家数字顺序记录。此轮只交付实现、配置、验证和本地执行命令，未启动正式训练。增加E会同时改变总容量及K/E，不能把差距扩大本身当作模型变好。

### 当前探索阶段暂不落盘最佳权重

按用户要求，专家数探索队列增加 `save_best_checkpoint=false`，由统一脚本传给所有组的 `train.save_best_checkpoint`。公共训练循环支持仅保留CPU内存最佳state_dict（参数和缓冲区独立拷贝），验证改善时覆盖内存快照，最后恢复该最佳轮模型测试。false时不建checkpoint目录、不保存模型或优化器；日志显式记录memory来源及best_weights_restored，成功跳过检查不再强制best.pt存在，但仍核对全部epoch、验证计划、最优轮和完整测试。其他未配置开关的历史训练默认仍落盘。

测试覆盖快照与训练模型不共享存储、缓冲区恢复、磁盘/内存两条完整训练测试路径指标一致、无checkpoint文件、完成识别及缺少恢复证据时拒绝。既有权重不删除，不中断用户正在运行的进程；新开关需要重启命令后生效，源码指纹改变会生成独立新队列目录。

## 2026-09-10深夜：两数据集截断/稠密路由机制对照

专家数探索表明E8等权test MAE=11.5537优于E8 Top-2=11.8405；不能将容量收益归因于前端Top-K。按用户“充分一点、明早9点、稍减训练样本并加入BikeNYC”的要求，新增 `configs/presets/dual_moe_routing_study.json`，继续复用同一个统一脚本，不改模型结构。

TaxiBJ同一等间隔2048/2491训练窗口、BikeNYC全量511训练窗口（本来已很小），完整验证/测试；E8等权、Top-2、Top-4、稠密softmax各42/2026双种子，另Top-4关闭前端均衡及稠密前端配等权后端各seed42，两数据集共20组。统一120epoch、val每2轮、batch4、最佳验证权重仅保留CPU内存。D8采用Top-8-of-8确保与已有路由头初始化一致，其前端硬计数均衡为常数故关闭；日志中D8硬分派均衡不代表概率均衡。

脚本支持不同数据集的预声明样本数验证、配对种子完整度/获胜数/百分比均值和样本标准差汇总；不把单种子机制组当作双种子。JSON默认截止北京时间2026-09-11 09:00，先测时并加15%余量，预计超预算不启动正式队列；这是准入检查而非强制终止，不保证硬截止，也不缩减某组epoch。完整组在过期截止时间后重运行仍可跳过/汇总。旧实验输出保留，本轮训练子集已变，不混用旧分数。用户本地运行，未代为启动正式实验。

## 2026-09-11：观测保留与目标条件组织读取

最新20组结果显示源Top-K没有稳定优于E8等权，密集前端也未稳定解决问题。结合Soft MoE、Attentive Neural Processes、SPIN和Switch论文，新增可选`target_readout_v1`：中/粗尺度各8个聚合专家都保留可见源输入，各自用自己的区域映射恢复到细目标位置后，用目标特征、候选内容、支持统计共享打分，目标Top-4融合；后端保留3尺度Top-2。前/后均衡0.001/0.01，120轮、val2、batch4，默认最佳参数不落盘，只保留CPU内存用于最终测试。

修改既有模型/损失/指标/日志模块，前端有效统计域从observed切至missing；旧配置和旧模型行为保留。新增集中预设`configs/presets/dual_moe_target.json`，复用`train_scale_completion.py`入口，支持前后uniform/topk的Q00–Q11对照。输出独立于旧实验；未改变历史队列或删除旧文件。均衡关闭与路由关闭耦合，故不声称是纯门控贡献实验。

新增单元测试和三数据集fixed/random共6组真实CUDA AMP小批次训练/验证/内存最佳恢复/测试已通过。特别核对：目标gate不影响聚合观测量；uniform新模型与旧E8 uniform参数和输出完全一致；前后端都能收到任务梯度。只执行功能检查，未启动正式精度实验，不保证此候选已经优于等权参考。详细依据见`model_designs/20260911_观测保留与目标条件双MoE改进依据.md`。

### 三数据集对照与分数据集轮数预算

按用户16:30前完成的要求，在同一个统一队列脚本新增`target-readout-three-dataset-v1`协议和`dual_moe_target_comparison.json`，random@0.4、seed42，三数据集各Q00/Q01/Q10/Q11共12组。TaxiBJ140轮/2048窗口，BikeNYC100轮/全511，CHAP150轮/2048；完整验证/测试、每2轮验证。各数据集epoch由JSON的dataset_epochs设置，禁止各候选单独改训练策略。全量数据原方案测时7.45小时不满足截止，所以在正式任务启动前统一缩减两较大数据集训练窗口，轮数不改。

测时公式改为读取实际任务配置epoch和val周期，输出每数据集预算；保留20%余量、截止准入、完整任务跳过、源指纹、内存最佳权重测试。自动汇总Q组配对效果和交互项，CSV记录实际epoch。新增队列配置、预算一致性、估时公式、缺组汇总和真实短队列恢复/跳过检查。用户本地执行正式训练；本次仅测时和功能验证。

### 独立种子复现与晚间软目标

三数据集seed42实验完成后，按用户“今晚20:00前、但不强制”的要求新增`dual_moe_target_replication.json`，只补TaxiBJ/BikeNYC各四Q组的seed2026，共8组。完全保持原轮数140/100、训练窗口2048/511和全部模型/优化超参数。新旧配置逐项匹配，仅seed和缓存/输出路径不同；旧结果单独保留，引用路径写入新JSON，队列不会重跑或假装自动合并seed42。

仅修改既有统一队列脚本，支持`deadline_mode=advisory`：超时预估只提示、继续完整训练。默认strict不变。新增测试验证8组覆盖、原参数匹配、非法模式拒绝、软目标过期仍全部调度且不缩减轮数。模型和损失代码未改，本轮不启动正式训练。CHAP负面结果不删除，后续另行诊断；两种子复现不能替代统计显著性或全数据集结论。

### 固定前端的后端机制验证（23:00预算）

针对BikeNYC双种子后端收益不稳定，新增`dual_moe_backend_study.json`，固定前端架构/超参数但仍端到端训练。BU后端等权、BD密集softmax（Top3-of-3）、BK0无均衡Top2、BK1有0.01均衡Top2。BD/BK0/BK1保持相同初始化，不引入新的模型代码。

统一队列新增`backend-routing-matched-v1`，按dataset_seeds调度：BikeNYC全511训练窗口、100轮、种子42/2026共8组优先，TaxiBJ等间隔1280窗口、140轮、seed42共4组。各数据集内部预算严格相同，完整验证/测试、内存最佳权重恢复测试。TaxiBJ不与历史2048样本分数直接比较。后端机制配对单独汇总，不生成不适用的2×2交互项。

GPU0测时约3.31小时（含20%余量），23:00严格预测准入，不能保证硬截止。新增前端不变/预算一致、不同数据集种子覆盖、稠密softmax及共同初始化、损失梯度、汇总缺组和真实短队列重启跳过检查。用户自行启动正式训练，本轮仅测时和检查。

### 全训练集、全部缺失条件的双 MoE 验证（2026-09-13 09:00目标）

按用户“优先充分性、全部训练样本”并将截止延至后天上午的要求，在原统一队列扩展`dual-moe-coverage-v1`，新增中央配置`dual_moe_coverage.json`，没有再建训练脚本或版本模型目录。默认117组：96组全条件四Q对照，12组random@0.4独立种子，9组SF/FD/SB机制控制；full与extended两种预算均使用所有2491/511/2626训练样本及完整验证/测试。

后端全组均衡为0，动态后端为Top3-of-3完整Softmax。新增前端可选static全局权重控制，只有该模式添加参数；旧uniform/learned/topk参数和初始化保持不变。FD与SF同为密集且无前端均衡，以免将条件权重作用和TopK稀疏作用混淆；Q11/Q01仍属于前端路由+均衡的整体消融，不能声称纯路由因果效应。

队列/缓存/源码身份支持多缺失率，任务键和CSV路径包含rate，汇总先按rate分层，避免跨缺失率错误配对。新增计划表、逐数据集等权条件汇总、尾部验证和最优轮次诊断。历史配置/结果保留；最佳参数仍只在CPU内存覆盖保留，最后恢复测试，无checkpoint落盘。

GPU0短测后默认batch16、TaxiBJ70/BikeNYC100/CHAP80轮、val每2轮，117组估算31.42小时（含20%余量）；extended为140/100/150，无截止。功能验证包含7种模型真实BikeNYC短训练→完整验证→最佳内存状态测试、所有rate覆盖/CSV对齐/不串配及旧模式回归。没有启动117组正式训练。未收敛、负面和种子翻转结果必须保留，不以完成一次实验保证idea成立。

### 2026-09-13 后端长预算确认与有来源的历史复用

根据117组结果，保持模型和loss源码不变，只扩展统一实验队列的`backend-confirmation-v1`协议及`dual_moe_backend_confirmation.json`。TaxiBJ random0.4/0.8各140轮，BikeNYC random0.4为100轮，均全训练/验证/测试，Q10/SB/Q11三后端×seed42/2026共18组；batch16、val每2轮、内存最佳权重测试、不落checkpoint、默认无截止。

复用仅限历史coverage中通过源码/预设、配置、CPU线程、原始数据身份、全量缓存内容、掩码及完整最佳测试检查的BikeNYC记录。目前5组可复用，实际新增13组。只允许队列文件自身hash变化；不豁免模型/损失/训练/配置构建代码。新汇总明确标注reused/trained及原run_dir，不伪造新日志或复制历史权重。参考文件身份进入新队列指纹，不合格则从头训练；--no-reuse可明确重跑18组。

按rate分层汇总三组配对、两seed统计及收敛诊断，不输出无意义的2×2交互；增加18组覆盖、13组真实调度计划、配置不匹配/源码或manifest变化拒绝复用、缺组汇总和三后端短训练→完整VAL→最佳TEST验证。没有启动正式长训练。

### 2026-09-13 后端受约束动态融合与机制诊断

依据18组结果新增可选completion_blend：none保持历史行为；fixed以0.5混合等权/动态；learned使用初始化0.5的全局sigmoid标量。没有改前端设计、专家数量或loss。旧默认state_dict与初始化保持不变，历史配置/结果不覆盖。

既有统一脚本接入backend-stability-v1，新增中央JSON，BikeNYC random0.4(100轮)、TaxiBJ random0.4/0.8(140轮)，U/D/H/L×42/2026/3407共36组，全量TRAIN/VAL/TEST、batch16、val每2轮、CPU最佳快照、不落参数、单卡串行。科学代码改变后不复用旧分数；同指纹已完整组可跳过，未完成组从头重跑。

新增可选detach诊断：三尺度原始误差相关/差异/分观测密度胜率与误差、实际权重集中率、alpha变化、总损失的裁剪前模块梯度范数，分别写入train/val/test.log和metrics.jsonl。空密度组与零方差相关不伪造数值。诊断真值不进入路由和loss。汇总按rate隔离六类配对，新增mechanism_summary.json。

验证包含四组前向/反向与内存恢复、历史默认精确等价、H/L初始化匹配、权重边界、alpha梯度、缺失真值不泄漏、诊断精确统计与分批不变性、36组计划/公平配置/不完整汇总以及可选真实BikeNYC短训练完整VAL/TEST集成测试。旧复用测试显式模拟归档科学身份，生产仍严格拒绝hash不匹配。未启动正式36组训练，方案及判断边界见全条件验证方案第9节。
