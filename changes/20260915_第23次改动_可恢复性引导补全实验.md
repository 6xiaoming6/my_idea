# V23 增量修改：目标相关可恢复性引导补全

- 开发分支：v23
- 修改前提交：398e550
- 修改后提交：待用户提交后填写（本次未执行Git提交）
- 日期：2026-09-15

在ST_DILATED主力基础上新增可关闭模块，不覆盖旧模型和实验配置。
以固定归一化时空基构造原始可见值的局部Gram矩阵与岭拟合，
用软未约束算子调节区域学习先验，再按每个专家自己的区域映射对齐到缺失目标，
将新局部信息交给原目标读出和ST_DILATED后端。新增同权重局部辅助损失及原始量纲诊断日志。

新增统一60组验证入口：`scripts/run_recoverability_experiments.py`。
四组主对照A/B/C/D加无学习先验局部拟合控制E；三个数据集，fixed/random，0.4/0.8，seed42。
全量训练/验证/测试，TaxiBJ140/BikeNYC100/CHAP150轮，val_epoch=2，最佳权重仅保留于CPU内存，最终恢复测试一次。
支持CPU机制检查、测速、dry-run、汇总、完整性审计和已完成任务跳过。

新增配置位于`configs/presets/`，代码位于原`src/stmoe_imputer/models/`，脚本位于原`scripts/`，
结果放入统一`outputs/v23/target_dual_moe/recoverability/`。没有新增版本代码文件夹。

工程验证不等于精度结论，本次不启动正式GPU实验。
完整说明：[实现与验证方案](../model_designs/v23_可恢复性引导双MoE实现与验证.md)。

## 同日补充：今晚22点的初筛预算

原60组配置保留，同一JSON增加`profiles.tonight`，同一脚本增加`--profile tonight`。
三个数据集、五组对照、fixed/random@0.8，共30组，保留全部样本。
BikeNYC60、CHAP50、TaxiBJ40轮，按此顺序运行；模型、损失、batch16和val_epoch=2不变。
新增测速后的截止时间准入检查，2026-09-15 22:00前预留30分钟；不自动砍epoch、不强制终止单组。
独立输出`recoverability/tonight/<指纹>`，仅作高缺失率、短预算方向筛选，非最终充分收敛结论。

后续按用户说明取消上述时间准入限制：22:00为非强制参考时间，超过目标只提示并继续正式队列，
目标日期过期也不阻止启动；保留测速、ETA和原预算，未自动缩短epoch。
增加主流程回归测试，验证过期目标和超时ETA均不阻止调用训练。
