# Script entry points

- `data/`：将项目数据转换为统一 benchmark 格式。
- `config/`：由策略生成各上游模型需要的 INI/YAML。
- `train/`：每个模型的薄适配入口与统一日志实现。
- `launch/`：用户应直接调用的批量训练和集成测试入口。

新增模型时应扩展现有配置生成器、训练适配器和启动器映射，不再在根目录创建
新的临时脚本或运行目录。
